import { PhotoModel } from "./photo";

export interface ApplicationUserModel {
    id: string;
    username: string;
    photoUrl: string;
    age: number;
    knownAs: string;
    created: Date;
    lastActive: Date;
    gender: string;
    introduction: string;
    lookingFor: string;
    interests: string;
    city: string;
    country: string;
    photos: PhotoModel[];
}